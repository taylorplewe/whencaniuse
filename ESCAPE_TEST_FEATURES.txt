css-case-insensitive - tests an edge case for a code block ending in a ] - just confuses the parsers
addeventlistener - both code blocks and links
details - contains the string "`<details>`" which really messes up the page if not escaped properly
document-currentscript - contains "`<string>`" in its description and can really mess up the parser if not done correctly
